apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-a.fullname" . }}-test-connection"
  labels:
    {{- include "demo-a.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-a.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
